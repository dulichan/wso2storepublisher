<%
var absolute = require ('modules/absolute.js').mvc;
var goose = require('modules/goose.js').goose;
var router = new goose({CONTEXT: "/publisher/"});
var log =new Log();

var mvc = new absolute({
	SERVER_URL:"/publisher/",
	IGNORE:["sso.jag", "login.jag"],
	API:"api",
	ROUTER: goose
});

router.get('users/', function(ctx){
	log.info("recieved");
	var users = [];
	users[0] = {id:1, name:'Chan'};
	print(users);
});

mvc.route(request);
%>